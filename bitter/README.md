# Bitter

Bitter is the Rust high-speed implementation. In Phase 0 it is only a checkable
scaffold. It must later match the external behavior fixed by the specification
layer and the OCaml reference implementation.
