# Golden Outputs

This directory records expected CLI outputs. Phase 0 keeps scaffold JSON files so
the comparison locations and validation harness exist before command behavior is
implemented.

Later phases must replace scaffold-only outputs with real normalized command
outputs generated by the OCaml reference implementation.
