# MonikaにおけるResourceとObservationの概念定義

## 1. この文書の範囲

この文書は、Monikaが観測対象をどのように指し、観測結果をどのような単位として扱うかを定義します。

この文書では、次の概念を扱います。

- `Resource`
- `Origin`
- `Observation`
- `ObservationType`
- `ObservationIdentity`
- `Interpreter`
- `Selector`
- `Region`
- `Failure`

この文書では、`Region`を一般的な`Resource`として扱いません。また、`Region`を`Observation`の代わりになる最小単位としても扱いません。`Region`は、一つの固定された`Observation`の全体または一部分を表すものとして扱います。

この文書は、概念間の関係と、各概念に必要な性質を定めます。データ構造、API、保存形式、通信形式、識別子の生成方法、キャッシュ方法などの実装は定めません。

この文書で「一回の処理」とは、Monikaが一つの入力に対する操作を開始してから、その操作の結果を確定するまでの範囲を指します。どこまでを一つの操作とするかは、各操作の定義に委ねます。

## 2. 用語

### 2.1 Resource

`Resource`は、Monikaが観測を試みる対象です。

`Resource`は、Monikaの内部にすでに固定された値である必要はありません。`Resource`の状態は時間とともに変わる場合があります。また、ある時点では観測できない場合もあります。

一つの`Resource`を複数回観測したとき、それぞれの観測が同じ結果を返すとは限りません。観測のたびに異なる`Observation`が得られる場合があります。

`Resource`自体について、有限であること、不変であること、内部構造を解釈できることは要求しません。これらのうち、有限性と処理中の不変性は`Observation`に要求します。

### 2.2 Origin

`Origin`は、同じ`Resource`を再び指そうとするための値です。

`Origin`は、ファイルのパスである必要はありません。この文書では、`Origin`の具体的な表現を定めません。

同じ正規化済みの`Origin`は、同じ`Resource`を指そうとしているものとして扱います。ここで正規化とは、`Origin`を比較に使う標準形へそろえることを指します。標準形と正規化方法は、この文書では定めません。

`Origin`が常に解決できることは要求しません。`Origin`からの観測は失敗できます。

この文書では、一つの`Resource`が複数の`Origin`を持てるかどうかや、二つの異なる`Origin`が同じ`Resource`を指せるかどうかを定めません。

### 2.3 Observation

この文書で観測とは、`Origin`が指そうとする`Resource`から、一回の処理で固定して扱える結果を得ようとすることです。

`Observation`は、一回の観測によって得られた結果です。

一つの`Observation`には、次の性質が必要です。

- 有限です。
- `ObservationType`を持ちます。
- 他の観測結果との同一性を判定できます。
- 一回の処理で使用している間は、固定された値として扱えます。
- その全体を表す`Region`を持ちます。

`Resource`の状態が後で変化した場合、その変化は既存の`Observation`を書き換えるものとして扱いません。新しい観測結果が必要であれば、別の`Observation`として扱います。

### 2.4 ObservationType

`ObservationType`は、`Observation`がどの種類の観測結果であるかを区別するための情報です。

この文書でいう型は、特定のプログラミング言語における静的型を意味しません。型の表現方法、型を決定する方法、型を検査する方法は定めません。

`ObservationType`を持つことは、`Observation`の内部構造を解釈できることを意味しません。内部構造を解釈できない観測結果も、型を持つ`Observation`として扱えます。

この文書では、`ObservationType`と`Interpreter`をどのように対応付けるかを定めません。

### 2.5 ObservationIdentity

`ObservationIdentity`は、二つの観測結果が同じ`Observation`であるかどうかを判定するための同一性です。

概念上は、次の判定が可能であることを要求します。

```text
sameObservation : Observation × Observation -> true | false
```

この文書では、`ObservationIdentity`をどのような値で表すかを定めません。SHA-256や内容の長さなどの特定の方式は要求しません。

二つの`Observation`が同じ同一性を持つと判断される場合、その二つは同じ`ObservationType`を持ち、Monikaがその`Observation`について返す内容も同じでなければなりません。

### 2.6 Interpreter

`Interpreter`は、固定された`Observation`と`Selector`の組から、その`Observation`に属する`Region`を特定するための解釈規則です。

`Interpreter`には、解釈規則を区別できる名前とバージョンが必要です。同じ名前であっても、バージョンが異なる場合には、異なる解釈規則として扱います。

この文書では、`Interpreter`の実行方法、配布方法、登録方法、選択方法を定めません。

### 2.7 Selector

`Selector`は、固定された`Observation`の全体または一部分を宣言的に指定するための値です。ここで宣言的な指定とは、対象へ到達するための操作手順ではなく、対象となる領域を特定するための記述を指します。

`Selector`だけでは、どの`Region`を指すかは決まりません。`Selector`の意味は、次の三つの組によって決まります。

```text
Interpreter
× Observation
× Selector
```

この三つから`Region`または`Failure`を得ることを、この文書では領域解決と呼びます。

この文書では、`Selector`の表現形式を定めません。

### 2.8 Region

`Region`は、一つの固定された`Observation`の全体または一部分を表します。

一つの`Region`は、必ず一つの`Observation`に属します。一つの`Region`が複数の`Observation`にまたがることは、この文書では認めません。

`Region`は、`Interpreter`と`Selector`による領域解決の結果になれます。また、`Observation`の全体を表す特別な`Region`も存在します。

この文書では、`Region`を独立した`Resource`として扱いません。また、`Region`を別の領域解決の基準として一般化しません。

### 2.9 Whole Region

`Whole Region`は、一つの`Observation`の全体を表す`Region`です。

すべての`Observation`には、少なくとも一つの`Whole Region`が必要です。`Observation`の内部構造を解釈できない場合でも、`Whole Region`は存在します。

`Whole Region`以外の細かい`Region`を持てることは、追加の能力です。すべての`Observation`に細かい`Region`を要求しません。

### 2.10 Failure

`Failure`は、観測または領域解決が結果を返せなかったことを表す結果です。

`Failure`は、定義された結果の一つです。この文書では、失敗理由の分類や表現形式を定めません。

## 3. 概念間の関係

概念間の関係は、次のように表せます。

```text
Origin
  └── Resourceを指そうとする
        └── 観測
              ├── Observation
              └── Failure

Observation
  ├── ObservationType
  ├── ObservationIdentity
  └── Whole Region

Interpreter
× Observation
× Selector
  └── 領域解決
        ├── Region
        └── Failure
```

関係を関数の形で簡略化すると、次のように書けます。

```text
observe : Origin -> Observation | Failure

wholeRegion : Observation -> Region

resolveRegion :
  Interpreter
  × Observation
  × Selector
  -> Region | Failure
```

これらは、必要な入力と結果の関係を示すための表記です。ソフトウェア上の関数、メソッド、通信手順を規定するものではありません。

## 4. Resourceの観測

### 4.1 観測は部分的にしか定義されません

すべての`Origin`から、常に`Observation`を取得できる必要はありません。

次のいずれも正当な結果です。

```text
observe(origin) -> Observation
observe(origin) -> Failure
```

同じ`Origin`に対する観測が、ある時点では成功し、別の時点では失敗する場合もあります。

### 4.2 同じResourceから複数のObservationを得られます

一つの`Resource`の状態が変化する場合、異なる時点の観測は異なる`Observation`になれます。

```text
Resource
  ├── Observation V1
  ├── Observation V2
  └── Observation V3
```

この関係によって、`Origin`が指そうとする対象と、その対象から得られた各時点の`Observation`を区別します。

### 4.3 Resourceの性質をObservationへ引き継ぐ必要はありません

`Resource`が変化可能であっても、得られた`Observation`は一回の処理中では固定値として扱えます。

`Resource`が継続的に存在すること、永続的に利用できること、または同じ内容を返し続けることは要求しません。

## 5. Observationに必要な性質

### 5.1 一つのObservationは有限です

一つの`Observation`が表す内容の量は有限です。

`Resource`自体が継続的または無期限に変化する対象であっても構いません。ただし、一回の観測によって得られる`Observation`には境界が必要です。後から新しく得られた内容は、既存の`Observation`へ追加するのではなく、別の`Observation`として扱います。

有限であることは、`Observation`の全内容を一度にメモリーへ読み込むことを要求しません。また、全内容を常に一つの連続したバイト列として表すことも要求しません。

この文書では、継続的な`Resource`から一回分の`Observation`の境界をどのように決めるかを定めません。

### 5.2 一つのObservationは型を持ちます

一つの`Observation`には、`ObservationType`が伴います。

型があることと、部分的な構造を解釈できることは別の性質です。型を持っていても、利用可能な`Interpreter`が存在しない場合があります。

### 5.3 観測状態の同一性を判定できます

Monikaは、二つの`Observation`が同じ観測結果であるかどうかを判定できる必要があります。

この判定によって、少なくとも次の区別が可能になります。

- 前回の観測から内容が変化したかどうか
- 参照が期待していた観測結果と現在の観測結果が同じかどうか
- ある処理の準備後に観測対象が変化したかどうか
- 同じ観測結果に対して同じ処理を再び適用しようとしているかどうか

この文書では、これらの判定を利用する処理の内容までは定めません。

### 5.4 一回の処理では固定された値として扱います

一回の処理が一つの`Observation`を使用している間、その`Observation`の型、同一性、および観測可能な内容は変化しないものとして扱います。

この性質は、`Resource`が不変であることを要求しません。`Resource`の状態が変わった場合には、必要に応じて新しい`Observation`を取得します。

## 6. アドレス可能性の分担

この文書では、対象を指すための仕組みを一つの概念へまとめません。対象の層ごとに異なる役割があります。

### 6.1 ResourceはOriginによって指します

`Origin`は、観測しようとする`Resource`を指します。

### 6.2 ObservationはObservationIdentityによって区別します

`ObservationIdentity`は、観測によって得られた固定結果を他の観測結果から区別します。

`Origin`と`ObservationIdentity`は同じ役割ではありません。同じ`Origin`から異なる`Observation`が得られる場合があります。

### 6.3 Observationの内容はRegionとして特定します

`Observation`の全体は`Whole Region`として特定できます。内部の一部分は、対応する`Interpreter`と`Selector`がある場合に、`Region`として特定できます。

この分担によって、次の三つを区別します。

```text
Resourceを指す値            = Origin
観測結果を区別する同一性    = ObservationIdentity
観測結果内の範囲を指定する値 = Selector
```

`Selector`から`Region`を得るためには、固定された`Observation`と`Interpreter`が必要です。

## 7. Regionの解決

### 7.1 Whole Regionは必ず存在します

すべての`Observation`は、その全体を表す`Whole Region`を持ちます。

内部構造が不明である場合や、利用可能な`Interpreter`が存在しない場合でも、この要件は変わりません。

### 7.2 部分Regionは追加の能力です

`Observation`の一部分を`Region`として特定できるかどうかは、利用可能な`Interpreter`と`Selector`によって決まります。

すべての`Observation`が、段落、行、要素、シンボルなどの細かい単位へ分解できる必要はありません。この文書では、そのような単位の種類も定めません。

### 7.3 Selectorの意味はInterpreterとObservationに依存します

同じ`Selector`であっても、`Interpreter`または`Observation`が異なれば、同じ意味になるとは限りません。

領域解決の入力は、次の組です。

```text
Interpreterの名前とバージョン
+ Observation
+ Selector
```

### 7.4 同じ入力には同じ結果が必要です

次の三つが同じである場合、領域解決は同じ`Region`を返すか、いずれも`Failure`になる必要があります。`Failure`の意味を区別する場合には、その意味も同じである必要があります。

- `Interpreter`の名前とバージョン
- `ObservationIdentity`
- `Selector`

`Resource`の現在状態は、この判定へ直接含めません。領域解決は、すでに固定された`Observation`に対して行います。

### 7.5 解決できないSelectorを補正しません

`Selector`が固定された`Observation`上で正確に解決できない場合、領域解決は`Failure`を返します。

近い位置にある別の対象、名前が似ている対象、以前の対象に似ている対象へ自動的に移動して成功したことにはしません。

領域解決とは別の自動修復を定義するかどうかは、この文書では定めません。自動修復によって得た対象を、領域解決そのものの成功結果としては扱いません。

### 7.6 Regionは一つのObservationに属します

領域解決によって得られた`Region`は、入力として使った一つの`Observation`に属します。

```text
observation(region) = input observation
```

一つの`Region`が別の`Observation`の内容を含むことや、複数の`Observation`をまたぐことは、この文書では定義しません。

## 8. この概念モデルが要求しない性質

`Resource`または`Observation`がMonikaで扱えるために、次の性質は要求しません。

- バイト列であること
- ファイルのパスを持つこと
- 人間が読めること
- 書き換え可能であること
- 永続的に存在すること
- 内部構造を解釈できること
- 全内容を一度に取り出せること
- 注釈を持つこと
- 他の対象との関係を持つこと
- 差分や変更操作を適用できること
- 他の観測結果を生成できること

`Observation`については、有限であること、型を持つこと、同一性を判定できること、一回の処理中に固定値として扱えること、および`Whole Region`を持つことを要求します。

## 9. 現行のArtifactという用語との関係

現行の資料では、観測結果に近い概念として`Artifact`という用語が使われています。

この文書では、`Artifact`を新しい概念として定義しません。また、`Artifact`を`Observation`の別名にするか、Monika内部における`Observation`の表現にするか、両者を別の概念として残すかを決めません。

そのため、この文書の関係式では`Observation`を使用します。`Artifact`との対応は、別途定義する必要があります。

## 10. この文書で定めない事項

次の事項は、この文書の範囲外です。

- `Origin`の構文と正規化方法
- `Resource`を取得または保持する方法
- 観測処理の実行方法
- `ObservationType`の表現、決定、検査の方法
- `ObservationIdentity`の表現と計算方法
- `Interpreter`の登録、選択、実行の方法
- `Selector`の構文と保存方法
- `Region`の内部表現と識別子の形式
- `Failure`の分類と伝達方法
- 継続的な`Resource`から有限な`Observation`を区切る方法
- キャッシュ、永続化、通信、並行実行に関する規則
- この概念モデルを利用する参照、注釈、関係、変更操作、生成操作の定義
- `Region`を別の`Resource`または領域解決の入力として扱う一般化

## 11. 要件の一覧

この文書で定める要件は、次のとおりです。

1. Monikaが観測する対象を`Resource`と呼びます。
2. `Resource`は`Origin`によって指されます。
3. `Origin`からの観測は、`Observation`または`Failure`を返します。
4. 一つの`Resource`から、異なる時点に異なる`Observation`を得られます。
5. 一つの`Observation`は有限です。
6. 一つの`Observation`は`ObservationType`を持ちます。
7. 二つの`Observation`が同じ観測結果であるかどうかを判定できます。
8. 一つの処理で使用している間、`Observation`は固定された値として扱われます。
9. すべての`Observation`は、その全体を表す`Whole Region`を持ちます。
10. 部分的な`Region`の解決は追加の能力であり、すべての`Observation`には要求しません。
11. 部分的な`Region`は、`Interpreter`、固定された`Observation`、および`Selector`の組から解決します。
12. 同じ`Interpreter`の名前とバージョン、同じ`ObservationIdentity`、および同じ`Selector`には、同じ`Region`が対応するか、いずれも`Failure`になります。
13. 正確に解決できない`Selector`は、別の対象へ補正せず、`Failure`として扱います。
14. 一つの`Region`は、一つの`Observation`に属します。
15. この文書では、`Region`を一般的な`Resource`または領域解決の入力として扱いません。
