{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://monika.local/schemas/region.schema.json",
  "title": "RegionDescriptor",
  "description": "Region observation introduced in version 3 and retained by version 6, with an artifact-local ID and optional interpreter for whole regions.",
  "$ref": "command-result.schema.json#/$defs/region"
}
