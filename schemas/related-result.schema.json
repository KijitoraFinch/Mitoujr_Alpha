{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://monika.local/schemas/related-result.schema.json",
  "title": "RelatedResult",
  "type": "object",
  "required": [
    "schemaVersion",
    "status",
    "query",
    "matches",
    "diagnostics",
    "coverage",
    "truncated"
  ],
  "properties": {
    "schemaVersion": {
      "const": "7"
    },
    "status": {
      "enum": ["ok", "incomplete", "failed"]
    },
    "query": {
      "type": "object",
      "required": [
        "observation",
        "direction",
        "limit"
      ],
      "properties": {
        "observation": {
          "$ref": "command-result.schema.json#/$defs/path"
        },
        "direction": {
          "enum": [
            "incoming",
            "outgoing",
            "both"
          ]
        },
        "predicate": {
          "type": "string",
          "minLength": 1
        },
        "region": {
          "type": "string",
          "minLength": 1
        },
        "regionScope": {
          "enum": ["exact", "contained"]
        },
        "limit": {
          "type": "integer",
          "minimum": 1,
          "maximum": 9007199254740991
        }
      },
      "dependentRequired": {
        "region": ["regionScope"],
        "regionScope": ["region"]
      },
      "additionalProperties": false
    },
    "matches": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/match"
      }
    },
    "diagnostics": {
      "type": "array",
      "items": {
        "$ref": "command-result.schema.json#/$defs/diagnostic"
      }
    },
    "coverage": {
      "$ref": "#/$defs/coverage"
    },
    "truncated": {
      "type": "boolean"
    }
  },
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": { "status": { "const": "ok" } }
      },
      "then": {
        "properties": {
          "diagnostics": {
            "not": {
              "contains": {
                "properties": {
                  "effectiveSeverity": { "const": "error" }
                },
                "required": ["effectiveSeverity"]
              }
            }
          },
          "coverage": {
            "properties": { "complete": { "const": true } }
          }
        }
      }
    },
    {
      "if": {
        "properties": { "status": { "const": "incomplete" } }
      },
      "then": {
        "properties": {
          "coverage": {
            "properties": { "complete": { "const": false } }
          }
        }
      }
    },
    {
      "if": {
        "properties": { "status": { "const": "failed" } }
      },
      "then": {
        "properties": {
          "matches": { "maxItems": 0 },
          "diagnostics": {
            "minItems": 1,
            "contains": {
              "properties": {
                "effectiveSeverity": { "const": "error" }
              },
              "required": ["effectiveSeverity"]
            }
          },
          "coverage": {
            "properties": {
              "primaryResources": { "const": 0 },
              "observed": { "const": 0 },
              "interpreted": { "const": 0 },
              "unsupported": { "const": 0 },
              "failed": { "const": 0 },
              "metadataDiscovered": { "const": 0 },
              "metadataDecoded": { "const": 0 },
              "metadataFailed": { "const": 0 },
              "complete": { "const": false }
            }
          },
          "truncated": { "const": false }
        }
      }
    }
  ],
  "$defs": {
    "match": {
      "type": "object",
      "required": [
        "direction",
        "kind",
        "predicate",
        "source",
        "target",
        "sourceResolution",
        "targetResolution"
      ],
      "properties": {
        "direction": {
          "enum": [
            "incoming",
            "outgoing",
            "internal"
          ]
        },
        "kind": {
          "enum": [
            "reference-use",
            "semantic-relation"
          ]
        },
        "predicate": {
          "type": "string",
          "minLength": 1
        },
        "source": {
          "$ref": "command-result.schema.json#/$defs/regionAddress"
        },
        "target": {
          "oneOf": [
            {
              "$ref": "command-result.schema.json#/$defs/regionAddress"
            },
            {
              "type": "object",
              "required": ["unresolvedReference"],
              "properties": {
                "unresolvedReference": {
                  "$ref": "command-result.schema.json#/$defs/originScopedId"
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "sourceResolution": {
          "enum": [
            "resolved",
            "unresolved",
            "invalid-selector",
            "unreadable",
            "not-checked"
          ]
        },
        "targetResolution": {
          "enum": [
            "resolved",
            "unresolved",
            "invalid-selector",
            "unreadable",
            "not-checked"
          ]
        },
        "evidence": {
          "$ref": "#/$defs/evidence"
        }
      },
      "additionalProperties": false
    },
    "evidence": {
      "type": "object",
      "minProperties": 1,
      "properties": {
        "reference": {
          "$ref": "command-result.schema.json#/$defs/originScopedId"
        },
        "annotation": {
          "$ref": "command-result.schema.json#/$defs/originScopedId"
        },
        "range": {
          "$ref": "command-result.schema.json#/$defs/range"
        }
      },
      "additionalProperties": false
    },
    "coverage": {
      "$ref": "command-result.schema.json#/$defs/coverage"
    }
  }
}
